#include <iostream>
#include <stack>
#include <queue>
#include "sets.h"

using namespace std;

int main() {
    /* BINARY SEARCH TREE

    BST tree;

    tree.insert(new Node(5));
    tree.insert(new Node(1));
    tree.insert(new Node(9));
    tree.insert(new Node(2));
    tree.insert(new Node(7));
    tree.insert(new Node(3));
    tree.insert(new Node(6));
    tree.insert(new Node(4));
    tree.insert(new Node(8));

    tree.display();
    tree.remove(9);
    cout << tree.search(10) << endl;

    Set set;
    std::set<char> s1 = {'c', 'd', 'c', 'a'};
    set.insertElement(s1, 'm');
    set.eraseElement(s1, 'a');
    set.display(s1);
    return 0;
*/

}
